// @flow

export type inputIntoA = {|
  could: boolean,
  be: number,
  anything: string
|};

export type resultFromA = {
    title: string,
    something: number,
    mightBeThere: ?string
}


export type inputIntoB = {|
  who: string,
  really: string,
  knows: ?string
|};

export type resultFromB = {
    longString: string
}